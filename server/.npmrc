# Text extraction (unpdf → pdf.js) declares `canvas` as an OPTIONAL dependency
# used only for PDF *rendering*, which we never do. Skipping optional deps keeps
# installs fast and `npm audit` clean (canvas is the source of the only advisories).
omit=optional
